

// let country = "usa"

// // country.toUpperCase() will not do anything

// // country = country.toUpperCase()
// // or
// console.log(country.toUpperCase())

const animalGroup = "frogs are very nice"
const animalGroupArray = animalGroup.split("o")
console.log(animalGroupArray)

const index = animalGroup.indexOf("very")
const slice = animalGroup.slice(index, index + 4)
console.log(slice)
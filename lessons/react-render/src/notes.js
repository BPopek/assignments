
// // mimics multiple pages 
//     npm i react-router-dom 
//     IMPORT IN PIECES -
//         browser router in index.js, then deconstruct and wrap APP file (all in index)
//         add Switch and Route in the App file

// Link used on Navbar, must say 'Link to '


// put the NAV above the switch because we always want it showing. Switch means only one of those pages is shown at a time.


 {/* <button onClick={() => props.history.push('/Products')}>Back</button>
        the props.history.push is automatically part of react render dom, must be in a callback function  */}